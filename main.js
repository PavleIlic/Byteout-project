var myCodeMirror = CodeMirror(document.getElementById('html__editor'), {
    lineNumbers: true,
    mode: "xml",
    theme: "ayu-mirage"
});

var myCodeMirror = CodeMirror(document.getElementById('js__editor'), {
    lineNumbers: true,
    mode: "javascript",
    theme: "ayu-mirage"
});

var myCodeMirror = CodeMirror(document.getElementById('css__editor'), {
    lineNumbers: true,
    mode: "css",
    theme: "ayu-mirage"
});